<!DOCTYPE html>
<html>
<body>
<% if @mystore != nil %>
    <!-- Page Content -->
    <div class="container">
        <div class="row">
            <div class="col-md-3">
                <p class="lead"><%= @mystore.name %></p>
                <div class="list-group">
                 <%= link_to 'Add Product', new_store_product_path(@mystore), :class => "list-group-item"%>

                    <%= link_to 'Edit Store Information', edit_store_path(@mystore.id), :class => "list-group-item"%>
                     
                     <%= link_to 'Delete My Store', store_path(@mystore.id),method: :delete, data: { confirm: 'Are you sure?' }, :class =>"list-group-item" %>
                </div>
            </div>

    <div class="col-md-9">
         <div class="row carousel-holder">
                <div class="col-md-8">
                    <div id="carousel-example-generic" class="carousel slide" data-ride="carousel">

                            <div class="carousel-inner">
                                <div class="item active" align="center">
                                    <%= link_to image_tag(root_url + "/images/" + @mystore.filename, size: "400x300", alt: @mystore.filename), @mystore %>
                                </div>
                             </div>
                        </div>
                    </div>
                </div>
             <% if @mystore.products != nil %>
                <div class="row">
                <% @mystore.products.each do |product| %>
                    <div class="col-sm-4 col-lg-4 col-md-4">
                        <div class="thumbnail">
                            <%= link_to image_tag(root_url + "/images/" + product.filename, size: "200x150", alt: product.filename), product %>
                            <div class="caption">
                                <h4 class="pull-right">$ <%= product.price %></h4>
                                <h4><%= product.name %></h4>
                                <p><%= product.description %></p>
                                <p>Quantity: <%= product.quantity %></p>
                            </div>
                        </div>
                    </div>
                <%end %>
                </div>
            <% end %>
        </div>
    </div>
     <% else %>
     <div class="form-group" align="center">
     <br> <br>
     <div class="alert alert-info">
  <strong>Info!</strong> Our records indicate that you do not have a store, if you wish to create one and sell products
    <%= link_to 'click here', new_store_path %> 
     </div>
      </div>
    <% end %>
</div>
</body>
</html>